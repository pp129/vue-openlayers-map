import { Canvas, Meta, Controls } from "@storybook/blocks";

import * as VHeatmap from "./VHeatmap.stories";

<Meta of={VHeatmap} />

# v-heatmap

热力图组件，用于可视化点数据的分布密度。

```js
import { VMap, VTile, VHeatmap } from "v-ol-map";
```

## 基础示例

> 根据点位权重显示热力分布

<Canvas of={VHeatmap.Default} />

## Docs

<Controls />
