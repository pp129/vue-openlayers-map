import { Canvas, Meta, Controls } from "@storybook/blocks";

import * as VGroupLayer from "./VGroupLayer.stories";

<Meta of={VGroupLayer} />

# v-group-layer

图层组组件，用于将多个图层组合在一起统一管理。

```js
import { VMap, VGroupLayer, VTile, VVector } from "v-ol-map";
```

## 基础示例

> 图层组包含多个矢量图层，可统一控制可见性和透明度

<Canvas of={VGroupLayer.Default} />

## Docs

<Controls />
